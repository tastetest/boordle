import react from "react"; 
import useEffect from "react";

const container = () => {

	const [word, setWord] = useEffect();

	// let's do some magic for the individual item
}

export default container;
